# Plaintext wedding content — never committed. The `d/*.bin` files
# (encrypted, generated by tools/build-invites.mjs) are what ships instead.
content/en.json
content/tr.json
content/de.json
content/details.json
tools/guests.csv
tools/links.csv
tools/*.secret

# Local config carrying the Apps Script auth token before it's baked into bundles
js/secrets.local.js

# OS / editor cruft
.DS_Store
*.log
node_modules/
